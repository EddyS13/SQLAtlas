<Window x:Class="DatabaseVisualizer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DatabaseVisualizer - Connect" 
        Height="550" 
        Width="700"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="CanResizeWithGrip">

    <Grid Background="{StaticResource WindowBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" 
        Background="{StaticResource PaneBackground}" 
        BorderBrush="{StaticResource BorderColor}" 
        BorderThickness="0,0,0,1" 
        Padding="20"
        MouseLeftButtonDown="WindowHeader_MouseLeftButtonDown">

            <Grid Height="50" Margin="10,0,10,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left">
                    <Image Source="/DatabaseVisualizer;component/Images/Database Visualizer Logo.png" 
                   Width="50" Height="50" Margin="0,0,10,0"
                   RenderOptions.BitmapScalingMode="HighQuality"/>
                    <TextBlock Text="DATABASE VISUALIZER" 
                       Foreground="{StaticResource AccentColor}" 
                       FontSize="28" 
                       FontWeight="ExtraBold" 
                       FontFamily="/DatabaseVisualizer;component/Fonts/#Roboto"
                       VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Content="—" Click="MinimizeButton_Click" Width="30" Height="30" Style="{StaticResource SystemButtonStyle}"/>
                    <Button Content="☐" Click="MaximizeRestoreButton_Click" Width="30" Height="30" Margin="5,0" Style="{StaticResource SystemButtonStyle}"/>
                    <Button Content="✕" Click="CloseButton_Click" Width="30" Height="30" Style="{StaticResource SystemButtonStyle}"/>
                </StackPanel>
            </Grid>
        </Border>

        <StackPanel Grid.Row="1" 
                VerticalAlignment="Center" 
                HorizontalAlignment="Center" 
                Width="350" 
                Background="{StaticResource PaneBackground}"
                Effect="{StaticResource DropShadowEffect}">

            <Border Padding="30">

                <StackPanel>
                    <StackPanel.Resources>
                        <Style TargetType="{x:Type TextBlock}">
                            <Setter Property="Foreground" Value="#202020"/>
                            <Setter Property="FontSize" Value="13"/>
                        </Style>
                    </StackPanel.Resources>

                    <TextBlock Text="Connect to SQL Server" 
                           FontSize="18" 
                           FontWeight="SemiBold" 
                           Margin="0,0,0,20"/>

                    <TextBlock Text="Server Name:" FontWeight="Bold" Margin="0,5,0,2"/>
                    <TextBox x:Name="ServerNameTextBox" Padding="5"/>

                    <TextBlock Text="Database Name:" FontWeight="Bold" Margin="0,15,0,2"/>
                    <TextBox x:Name="DatabaseNameTextBox" Padding="5"/>

                    <TextBlock Text="Username:" FontWeight="Bold" Margin="0,5,0,2"/>
                    <TextBox x:Name="UsernameTextBox" IsEnabled="False" Padding="5"/>

                    <TextBlock Text="Password:" FontWeight="Bold" Margin="0,15,0,2"/>
                    <PasswordBox x:Name="PasswordBox" IsEnabled="False" Padding="5"/>

                    <CheckBox x:Name="WindowsAuthCheckBox" 
                          Content="Use Windows Authentication" 
                          IsChecked="True" 
                          Click="WindowsAuthCheckBox_Toggled"  
                          Margin="0,15,0,10"/>
                    
                    <Button x:Name="ConnectButton" 
                        Content="Connect" 
                        Margin="0,25,0,10" 
                        Click="ConnectButton_Click"/>

                    <TextBlock x:Name="StatusTextBlock" 
                           Text="" 
                           Foreground="Red" 
                           TextWrapping="Wrap" 
                           Margin="0,10,0,0"/>
                </StackPanel>

            </Border>
        </StackPanel>
    </Grid>
</Window>