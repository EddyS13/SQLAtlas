<UserControl x:Class="SQLAtlas.Views.DriveSpaceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SQLAtlas.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    
    <DockPanel Margin="10">
        <TextBlock DockPanel.Dock="Top" Margin="0,0,0,10" FontWeight="ExtraBold" FontSize="16"
                   Foreground="{StaticResource AccentColor}"
                   Text="Physical Drive Space &amp; Database File Locations"/>

        <Button Content="Refresh Drive Status" DockPanel.Dock="Top" Margin="0,0,0,10"
                x:Name="RefreshDriveButton" Click="RefreshDriveButton_Click"/>

        <TextBlock DockPanel.Dock="Top" Text="Monitored Drives and Free Space" FontWeight="Bold" Margin="0,0,0,5"/>

        <DataGrid x:Name="DriveSpaceGrid" 
                  HeadersVisibility="Column" 
                  AutoGenerateColumns="True"
                  CanUserAddRows="False"
                  Height="Auto"
                  MaxHeight="400"/>
    </DockPanel>
</UserControl>
