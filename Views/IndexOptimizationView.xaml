<UserControl x:Class="SQLAtlas.Views.IndexOptimizationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:SQLAtlas.Converters" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1100">

    <UserControl.Resources>
        <local:ScriptToEnabledConverter x:Key="ScriptToEnabledConverter"/>
    </UserControl.Resources>

    <Grid Background="{StaticResource DeepBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource CardBackground}" CornerRadius="12" Margin="20,20,20,10" Padding="25">
            <Grid>
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⚡" Foreground="{StaticResource AccentColor}" FontSize="22" Margin="0,0,15,0" VerticalAlignment="Center"/>
                        <TextBlock Text="INDEX OPTIMIZATION ADVISOR" FontSize="24" FontWeight="Bold" Foreground="{StaticResource FontColor}"/>
                    </StackPanel>
                    <TextBlock x:Name="IndexStatusTextBlock" Text="READY FOR ANALYSIS" Foreground="{StaticResource MutedFontColor}" Margin="37,5,0,0" FontSize="13" FontWeight="SemiBold" Opacity="0.7"/>
                </StackPanel>

                <Button x:Name="RefreshIndexButton" HorizontalAlignment="Right" VerticalAlignment="Center"
                        Padding="20,10" Click="RefreshIndexButton_Click"
                        Background="{StaticResource BorderColor}" Foreground="{StaticResource FontColor}" 
                        FontWeight="SemiBold" BorderThickness="0" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                    <TextBlock Text="REFRESH ANALYSIS"/>
                </Button>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="{StaticResource CardBackground}" CornerRadius="12" Margin="20,0,20,10" Padding="20">
            <Grid>
                <DataGrid x:Name="FragmentedIndexDataGrid" 
                          AlternatingRowBackground="{StaticResource GridAlternateRowBackground}"
                          AutoGenerateColumns="False" Background="Transparent" BorderThickness="0"
                          CanUserAddRows="False" SelectionMode="Single"
                          PreviewMouseWheel="DataGrid_PreviewMouseWheel">

                    <DataGrid.Resources>
                        <StaticResource x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" ResourceKey="AccentColor"/>
                        <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="White"/>
                    </DataGrid.Resources>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="TABLE" Binding="{Binding SchemaTableName}" Width="1.5*"/>
                        <DataGridTextColumn Header="INDEX NAME" Binding="{Binding IndexName}" Width="2*"/>

                        <DataGridTemplateColumn Header="FRAG %" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="10" Padding="8,2" HorizontalAlignment="Left" x:Name="FragPill" Background="#22C55E">
                                        <TextBlock Text="{Binding FragmentationPercent, StringFormat={}{0:F1}%}" FontSize="10" FontWeight="Bold" Foreground="White"/>
                                    </Border>
                                    <DataTemplate.Triggers>
                                        <DataTrigger Binding="{Binding MaintenanceAction}" Value="REORGANIZE">
                                            <Setter TargetName="FragPill" Property="Background" Value="#F59E0B"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding MaintenanceAction}" Value="REBUILD">
                                            <Setter TargetName="FragPill" Property="Background" Value="#EF4444"/>
                                        </DataTrigger>
                                    </DataTemplate.Triggers>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTextColumn Header="PAGES" Binding="{Binding PageCount, StringFormat={}{0:N0}}" Width="90"/>

                        <DataGridTemplateColumn Header="ACTION" Width="140">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Execute Repair" 
                                            Style="{StaticResource ActionButtonStyle}"
                                            Click="MaintenanceActionButton_Click" 
                                            CommandParameter="{Binding MaintenanceScript}" 
                                            IsEnabled="{Binding MaintenanceScript, Converter={StaticResource ScriptToEnabledConverter}}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <TextBlock Grid.Row="2" Margin="30,0,0,20" FontSize="11" Foreground="{StaticResource MutedFontColor}" Opacity="0.6"
                   Text="NOTE: ONLY INDEXES WITH PAGE_COUNT > 8 ARE EVALUATED FOR MAINTENANCE RECOMMENDATIONS."/>
    </Grid>
</UserControl>