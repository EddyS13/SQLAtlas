<UserControl x:Class="SQLAtlas.Views.ConfigurationOverview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="800" d:DesignWidth="1200">

    <Grid Margin="40">
        <StackPanel>
            <TextBlock Text="SERVER CONFIGURATION" FontSize="32" FontWeight="Bold" Foreground="White" Margin="0,0,0,10"/>
            <TextBlock Text="Environment identity and instance-level properties." 
                       Foreground="{StaticResource MutedFontColor}" FontSize="16" Margin="0,0,0,30"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="{StaticResource CardBackground}" CornerRadius="12" Padding="25" Margin="0,0,10,0">
                    <StackPanel>
                        <TextBlock Text="SERVER IDENTITY" Foreground="{StaticResource AccentColor}" FontWeight="Bold" FontSize="12" Margin="0,0,0,15"/>
                        <ItemsControl x:Name="ServerIdentityList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding Key}" Foreground="{StaticResource MutedFontColor}" FontSize="13"/>
                                        <TextBlock Grid.Column="1" Text="{Binding Value}" Foreground="White" FontWeight="SemiBold" 
                   HorizontalAlignment="Right" FontSize="13"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Background="{StaticResource CardBackground}" CornerRadius="12" Padding="25" Margin="5,0,5,0">
                    <StackPanel>
                        <TextBlock Text="RESOURCES &amp; PERFORMANCE" Foreground="{StaticResource AccentColor}" FontSize="12" FontWeight="Bold" Margin="0,0,0,15"/>

                        <TextBlock x:Name="VersionHeaderTextBlock" 
                                   Text="Loading version info..." 
                                   Foreground="White" 
                                   FontSize="15" 
                                   FontWeight="Bold" 
                                   TextWrapping="Wrap" 
                                   Margin="0,0,0,15"/>

                        <ItemsControl x:Name="EnvironmentStatsList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding Key}" Foreground="{StaticResource MutedFontColor}" FontSize="13"/>
                                        <TextBlock Grid.Column="1" Text="{Binding Value}" Foreground="White" FontWeight="SemiBold" 
                   HorizontalAlignment="Right" FontSize="13"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Background="{StaticResource CardBackground}" CornerRadius="12" Padding="25" Margin="10,0,0,0">
                    <StackPanel>
                        <TextBlock Text="CONFIGURATION ADVISOR" Foreground="{StaticResource AccentColor}" FontSize="12" FontWeight="Bold" Margin="0,0,0,15"/>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <TextBlock x:Name="AdvisorStatusIcon" Text="🔄" FontSize="20" Margin="0,0,10,0"/>
                            <TextBlock x:Name="AdvisorTitle" Text="Scanning..." Foreground="White" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock x:Name="AdvisorDesc" Text="Scanning for bottlenecks..." Foreground="{StaticResource MutedFontColor}" 
                                   TextWrapping="Wrap" FontSize="13" LineHeight="20"/>
                    </StackPanel>
                </Border>
            </Grid>

            <TextBlock Text="CONFIGURATION TOOLS" Foreground="White" FontWeight="Bold" Margin="10,40,0,10"/>
            <UniformGrid Columns="2">
                <Button Click="ToolBlock_Click" Tag="Server/Database Configuration Editor" Style="{StaticResource EmptyButtonStyle}" Margin="10">
                    <Border Background="{StaticResource CardBackground}" CornerRadius="12" Padding="20">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="50"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="⚙️" FontSize="30" VerticalAlignment="Center"/>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0,0,0">
                                <TextBlock Text="Server Configuration Editor" FontWeight="Bold" FontSize="18" Foreground="White"/>
                                <TextBlock Text="Modify memory, parallelism, and advanced instance options." Foreground="{StaticResource MutedFontColor}" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Button>

                <Button Click="ToolBlock_Click" Tag="Drive Space and Growth Monitor" Style="{StaticResource EmptyButtonStyle}" Margin="10">
                    <Border Background="{StaticResource CardBackground}" CornerRadius="12" Padding="20">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="50"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="📂" FontSize="30" VerticalAlignment="Center"/>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0,0,0">
                                <TextBlock Text="Drive Space &amp; Growth" FontWeight="Bold" FontSize="18" Foreground="White"/>
                                <TextBlock Text="View recovery models, file paths, and compatibility levels." Foreground="{StaticResource MutedFontColor}" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Button>
            </UniformGrid>
        </StackPanel>
    </Grid>
</UserControl>