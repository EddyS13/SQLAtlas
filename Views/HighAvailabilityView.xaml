<UserControl x:Class="SQLAtlas.Views.HighAvailabilityView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1100">

    <Grid Background="{StaticResource DeepBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource CardBackground}" CornerRadius="12" Margin="20,20,20,10" Padding="25">
            <Grid>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🔗" FontSize="22" Margin="0,0,15,0" VerticalAlignment="Center"/>
                    <StackPanel>
                        <TextBlock Text="ALWAYS ON AVAILABILITY GROUPS" FontSize="24" FontWeight="Bold" Foreground="{StaticResource FontColor}"/>
                        <TextBlock Text="REPLICA SYNCHRONIZATION &amp; FAILOVER STATUS" Foreground="{StaticResource MutedFontColor}" FontSize="12" FontWeight="SemiBold" Opacity="0.7"/>
                    </StackPanel>
                </StackPanel>

                <Button x:Name="RefreshAGButton" HorizontalAlignment="Right" VerticalAlignment="Center"
                        Style="{StaticResource ActionButtonStyle}" Padding="20,10" Click="RefreshAGButton_Click"
                        Content="REFRESH AG STATUS"/>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="{StaticResource CardBackground}" CornerRadius="12" Margin="20,0,20,20" Padding="20">
            <Grid>
                <StackPanel x:Name="NoDataTextBlock" Visibility="Collapsed" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock x:Name="NoDataStatusText" 
                        Text="⚠️ STANDALONE INSTANCE DETECTED" 
                        Foreground="{StaticResource AccentColor}" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center"/>

                    <TextBlock Text="Always On Availability Groups are not configured on this server." 
                        Foreground="White" Opacity="0.7" Margin="0,10,0,20" HorizontalAlignment="Center"/>

                </StackPanel>

                <DataGrid x:Name="AgStatusGrid" AutoGenerateColumns="False" 
                          Background="Transparent" BorderThickness="0" CanUserAddRows="False"
                          AlternatingRowBackground="{StaticResource GridAlternateRowBackground}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="AG NAME" Binding="{Binding AGName}" Width="1.2*"/>
                        <DataGridTextColumn Header="REPLICA NODE" Binding="{Binding ReplicaServerName}" Width="1.5*"/>
                        <DataGridTextColumn Header="ROLE" Binding="{Binding Role}" Width="100"/>

                        <DataGridTemplateColumn Header="SYNC STATUS" Width="140">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="10" Padding="8,2" HorizontalAlignment="Left" x:Name="StatusPill" Background="#22C55E">
                                        <TextBlock Text="{Binding SynchronizationState}" FontSize="10" FontWeight="Bold" Foreground="White"/>
                                    </Border>
                                    <DataTemplate.Triggers>
                                        <DataTrigger Binding="{Binding SynchronizationState}" Value="NOT SYNCHRONIZING">
                                            <Setter TargetName="StatusPill" Property="Background" Value="#EF4444"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding SynchronizationState}" Value="SYNCHRONIZING">
                                            <Setter TargetName="StatusPill" Property="Background" Value="#F59E0B"/>
                                        </DataTrigger>
                                    </DataTemplate.Triggers>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTextColumn Header="FAILOVER MODE" Binding="{Binding FailoverMode}" Width="130"/>

                        <DataGridTemplateColumn Header="HEALTH" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="●" FontSize="18" HorizontalAlignment="Center" x:Name="HealthDot" Foreground="#22C55E"/>
                                    <DataTemplate.Triggers>
                                        <DataTrigger Binding="{Binding OperationalState}" Value="CRITICAL">
                                            <Setter TargetName="HealthDot" Property="Foreground" Value="#EF4444"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding OperationalState}" Value="WARNING">
                                            <Setter TargetName="HealthDot" Property="Foreground" Value="#F59E0B"/>
                                        </DataTrigger>
                                    </DataTemplate.Triggers>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>