<UserControl x:Class="SQLAtlas.Views.HighAvailabilityView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SQLAtlas.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    
    <DockPanel Margin="10">
        <TextBlock DockPanel.Dock="Top" Margin="0,0,0,10" FontWeight="ExtraBold" FontSize="16"
                   Foreground="{StaticResource AccentColor}"
                   Text="Always On Availability Group Status"/>

        <Button Content="Refresh AG Status" DockPanel.Dock="Top" Margin="0,0,0,10"
                x:Name="RefreshAGButton" Click="RefreshAGButton_Click"/>

        <TextBlock DockPanel.Dock="Top" Text="Replica Synchronization Status" FontWeight="Bold" Margin="0,0,0,5"/>

        <DataGrid x:Name="AgStatusGrid" 
          HeadersVisibility="Column" 
          AutoGenerateColumns="True"
          CanUserAddRows="False"
          Height="Auto"
          MaxHeight="400"
          Margin="0,0,0,10"/>

        <TextBlock x:Name="NoDataTextBlock"
           Text="— No Availability Group replicas found. (Is Always On configured?) —"
           FontStyle="Italic"
           HorizontalAlignment="Center"
           VerticalAlignment="Top"
           Margin="0, 30, 0, 0"
           Foreground="{StaticResource FontColor}"
           Visibility="Collapsed"/>
    </DockPanel>
</UserControl>