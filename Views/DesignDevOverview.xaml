<UserControl x:Class="SQLAtlas.Views.DesignDevOverview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">

    <Grid Margin="40">
        <StackPanel>
            <TextBlock Text="DESIGN &amp; DEVELOPMENT" FontSize="32" FontWeight="Bold" Foreground="White" Margin="0,0,0,10"/>
            <TextBlock Text="Tools for database architectural analysis and optimization." Foreground="{StaticResource MutedFontColor}" FontSize="16" Margin="0,0,0,40"/>

            <UniformGrid Columns="3">
                <Button Click="ToolBlock_Click" Tag="Schema Comparison" Style="{StaticResource EmptyButtonStyle}">
                    <Border Background="{StaticResource CardBackground}" CornerRadius="12" Padding="20" Margin="10">
                        <StackPanel>
                            <TextBlock Text="🔄" FontSize="40" Margin="0,0,0,10"/>
                            <TextBlock Text="Schema Compare" FontWeight="Bold" FontSize="18" Foreground="White"/>
                            <TextBlock Text="Sync structures between environments." Foreground="{StaticResource MutedFontColor}" TextWrapping="Wrap" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>
                </Button>

                <Button Click="ToolBlock_Click" Tag="SQL Snippet/Template Library" Style="{StaticResource EmptyButtonStyle}">
                    <Border Background="{StaticResource CardBackground}" CornerRadius="12" Padding="20" Margin="10">
                        <StackPanel>
                            <TextBlock Text="📚" FontSize="40" Margin="0,0,0,10"/>
                            <TextBlock Text="Snippet Library" FontWeight="Bold" FontSize="18" Foreground="White"/>
                            <TextBlock Text="Common DBA scripts and templates." Foreground="{StaticResource MutedFontColor}" TextWrapping="Wrap" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>
                </Button>

                <Button Click="ToolBlock_Click" Tag="Query Execution Plan Visualizer" Style="{StaticResource EmptyButtonStyle}">
                    <Border Background="{StaticResource CardBackground}" CornerRadius="12" Padding="20" Margin="10">
                        <StackPanel>
                            <TextBlock Text="⚡" FontSize="40" Margin="0,0,0,10"/>
                            <TextBlock Text="Query Visualizer" FontWeight="Bold" FontSize="18" Foreground="White"/>
                            <TextBlock Text="Analyze T-SQL execution plans." Foreground="{StaticResource MutedFontColor}" TextWrapping="Wrap" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>
                </Button>
            </UniformGrid>
        </StackPanel>
    </Grid>
</UserControl>