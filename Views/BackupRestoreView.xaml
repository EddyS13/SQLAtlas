<UserControl x:Class="SQLAtlas.Views.BackupRestoreView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SQLAtlas.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <DockPanel Margin="10">
        <TextBlock DockPanel.Dock="Top" Margin="0,0,0,10" FontWeight="ExtraBold" FontSize="16"
                   Foreground="{StaticResource AccentColor}"
                   Text="Backup History / Restore Manager"/>

        <Button Content="Refresh Backup History" DockPanel.Dock="Top" Margin="0,0,0,10"
                x:Name="RefreshBackupButton" Click="RefreshBackupButton_Click"/>

        <TextBlock DockPanel.Dock="Top" Text="Recent Backups (Last 20)" FontWeight="Bold" Margin="0,0,0,5"/>

        <DataGrid x:Name="BackupHistoryGrid" 
                  AutoGenerateColumns="True" 
                  CanUserAddRows="False" 
                  DockPanel.Dock="Top" 
                  Height="400"
                  SelectionChanged="BackupHistoryGrid_SelectionChanged"/>

        <TextBlock Text="Selected Backup Restore Script" FontWeight="Bold" Margin="0,15,0,5"/>
        <TextBox x:Name="RestoreScriptTextBox" 
                 IsReadOnly="True" 
                 Height="100" 
                 TextWrapping="Wrap" 
                 FontFamily="Consolas"
                 AcceptsReturn="True"
                 Background="{StaticResource WindowBackground}"
                 Foreground="{StaticResource FontColor}"
                 BorderBrush="{StaticResource BorderColor}"
                 BorderThickness="1"
                 Padding="5"/>
    </DockPanel>
</UserControl>