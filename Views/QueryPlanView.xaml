<UserControl x:Class="SQLAtlas.Views.QueryPlanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SQLAtlas.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel Margin="10">
        <TextBlock DockPanel.Dock="Top" Margin="0,0,0,10" FontWeight="ExtraBold" FontSize="16"
                   Foreground="{StaticResource AccentColor}"
                   Text="Query Execution Plan Visualizer"/>

        <TextBlock DockPanel.Dock="Top" Text="1. Enter Query to Analyze" FontWeight="Bold" Margin="0,5,0,5"/>
        <Grid DockPanel.Dock="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="QueryInputTextBox" 
                     Grid.Column="0" 
                     Height="120" 
                     TextWrapping="Wrap" 
                     AcceptsReturn="True"
                     FontFamily="Consolas" 
                     FontSize="12"
                     Text="SELECT TOP 10 * FROM sys.objects;"/>

            <Button Content="⚡ Get Plan" 
                    Grid.Column="1" 
                    x:Name="GetPlanButton" 
                    Click="GetPlanButton_Click"
                    VerticalAlignment="Top"
                    Margin="10,0,0,0"
                    Padding="10"/>
        </Grid>

        <Grid DockPanel.Dock="Top" Margin="0,20,0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" FontWeight="Bold" Foreground="{StaticResource AccentColor}"
               Text="2. Execution Plan XML (Raw Output)"/>

            <Button Grid.Column="1" 
            x:Name="CopyPlanButton" 
            Content="📋 Copy XML" 
            Click="CopyPlanButton_Click"
            HorizontalAlignment="Right"
            Width="120"
            Padding="5"/>
        </Grid>

        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <TextBox x:Name="PlanOutputTextBox" 
                     IsReadOnly="True" 
                     TextWrapping="NoWrap" 
                     FontFamily="Consolas" 
                     FontSize="11" 
                     Background="{StaticResource WindowBackground}"
                     BorderBrush="{StaticResource BorderColor}"
                     BorderThickness="1"/>
        </ScrollViewer>
    </DockPanel>
</UserControl>