<UserControl x:Class="SQLAtlas.Views.ExpensiveQueriesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1100">

    <Grid Background="{StaticResource DeepBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource CardBackground}" CornerRadius="12" Margin="20,20,20,10" Padding="25">
            <Grid>
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📉" Foreground="{StaticResource AccentColor}" FontSize="22" Margin="0,0,15,0" VerticalAlignment="Center"/>
                        <TextBlock Text="TOP EXPENSIVE QUERIES" FontSize="24" FontWeight="Bold" Foreground="{StaticResource FontColor}"/>
                    </StackPanel>
                    <TextBlock Text="HISTORICAL RESOURCE ANALYSIS (BY I/O &amp; READS)" Foreground="{StaticResource MutedFontColor}" Margin="37,5,0,0" FontSize="13" FontWeight="SemiBold" Opacity="0.7"/>
                </StackPanel>

                <Button x:Name="RefreshPerformanceButton" 
                        HorizontalAlignment="Right" VerticalAlignment="Center"
                        Padding="20,10" Click="RefreshPerformanceButton_Click"
                        Background="{StaticResource BorderColor}" Foreground="{StaticResource FontColor}" 
                        FontWeight="SemiBold" BorderThickness="0" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                    <TextBlock Text="REFRESH ANALYSIS"/>
                </Button>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="{StaticResource CardBackground}" CornerRadius="12" Margin="20,0,20,20" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="TOP 10 PLAN CACHE HOGS" Foreground="{StaticResource AccentColor}" FontSize="11" FontWeight="Bold" Margin="0,0,0,15" Opacity="0.8"/>

                <DataGrid x:Name="ExpensiveQueriesDataGrid" Grid.Row="1"
          AlternatingRowBackground="{StaticResource GridAlternateRowBackground}"
          AutoGenerateColumns="False" 
          Background="Transparent" 
          BorderThickness="0"
          VerticalContentAlignment="Center">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="EXECS" Binding="{Binding ExecutionCount}" Width="70"/>
                        <DataGridTextColumn Header="AVG CPU" Binding="{Binding AvgCpuTime, StringFormat={}{0}ms}" Width="90"/>
                        <DataGridTextColumn Header="AVG READS" Binding="{Binding AvgLogicalReads, StringFormat={}{0:N0}}" Width="110"/>

                        <DataGridTemplateColumn Header="QUERY TEXT" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding CleanQueryText}" 
                               FontFamily="Consolas" 
                               FontSize="12"
                               TextTrimming="CharacterEllipsis" 
                               Foreground="{StaticResource CodeForeground}"
                               VerticalAlignment="Center"
                               ToolTip="{Binding QueryText}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Width="80">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="COPY" Click="CopyQuery_Click"
                            Background="{StaticResource BorderColor}" 
                            Foreground="{StaticResource MutedFontColor}"
                            FontSize="10" FontWeight="Bold" Padding="8,2" Cursor="Hand">
                                        <Button.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="4"/>
                                            </Style>
                                        </Button.Resources>
                                    </Button>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <TextBlock x:Name="NoDataText" Grid.Row="1" Text="No performance data found in plan cache." 
                           Foreground="{StaticResource MutedFontColor}" FontStyle="Italic"
                           HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>