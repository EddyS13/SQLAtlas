<UserControl x:Class="SQLAtlas.Views.AuditLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SQLAtlas.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="10">

            <TextBlock Text="Recent Security Events (Failed Logins/Errors)" 
                   FontWeight="ExtraBold" 
                   FontSize="16" 
                   Foreground="{StaticResource AccentColor}" 
                   Margin="0,0,0,10"/>

            <Grid Height="Auto" MaxHeight="300" Margin="0,0,0,15">

                <DataGrid x:Name="AuditLogGrid" 
              HeadersVisibility="Column" 
              Height="Auto" 
              MaxHeight="300" 
              CanUserAddRows="False"/>

                <TextBlock x:Name="NoLogsTextBlock" 
               Text="— Audit logs are empty or access is restricted. —" 
               FontStyle="Italic" 
               HorizontalAlignment="Center" 
               VerticalAlignment="Center" 
               Foreground="{StaticResource FontColor}"
               Visibility="Collapsed"/>

            </Grid>

        </StackPanel>
    </ScrollViewer>
</UserControl>
