<UserControl x:Class="DatabaseVisualizer.Views.SecurityView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="10">

            <TextBlock Text="Database User and Role Audit" 
                       FontWeight="ExtraBold" 
                       FontSize="16" 
                       Foreground="{StaticResource AccentColor}" 
                       Margin="0,0,0,10"/>

            <TextBlock Text="Active Database Principals and Roles" FontWeight="Bold" Margin="0,5,0,5"/>
            <DataGrid x:Name="UsersRolesGrid" 
                      HeadersVisibility="Column" 
                      AutoGenerateColumns="True"
                      Height="250" 
                      CanUserAddRows="False"/>

            <Separator Margin="0,20"/>

            <TextBlock Text="Recent Security Events (Failed Logins/Errors)" FontWeight="Bold" Margin="0,5,0,5"/>
            <DataGrid x:Name="AuditLogGrid" 
                      HeadersVisibility="Column" 
                      AutoGenerateColumns="True"
                      Height="250" 
                      CanUserAddRows="False"/>

            <Separator Margin="0,20"/>

            <TextBlock Text="Data Masking and Column Encryption Status" FontWeight="Bold" Margin="0,5,0,5"/>
            <TextBlock Text="Data masking configuration and encryption status will appear here." FontStyle="Italic" Margin="0,10,0,0"/>

        </StackPanel>
    </ScrollViewer>
</UserControl>