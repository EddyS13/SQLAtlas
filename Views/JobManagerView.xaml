<UserControl x:Class="SQLAtlas.Views.JobManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <DockPanel Margin="10">
        <TextBlock DockPanel.Dock="Top" Margin="0,0,0,10" FontWeight="ExtraBold" FontSize="16"
                   Foreground="{StaticResource AccentColor}"
                   Text="SQL Agent Job Scheduling/Monitor"/>

        <Button Content="Refresh Job Status" 
                DockPanel.Dock="Top" 
                Margin="0,0,0,10"
                Padding="5"
                x:Name="RefreshJobsButton" 
                Click="RefreshJobsButton_Click"/>

        <DataGrid x:Name="JobsDataGrid" 
                  HeadersVisibility="Column" 
                  AutoGenerateColumns="True"
                  CanUserAddRows="False"/>
    </DockPanel>
</UserControl>