<Window x:Class="SQLAtlas.ExplorerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:System="clr-namespace:System;assembly=mscorlib"
        xmlns:local="clr-namespace:SQLAtlas.Models"
        Title="SQL Atlas" 
        Height="768" Width="1024"
        WindowStyle="None"                  
        AllowsTransparency="True"
        ResizeMode="CanResize"
        Background="{StaticResource WindowBackground}">

    <Window.Resources>
        <Style TargetType="{x:Type ListBoxItem}">
            <Setter Property="Background" Value="{StaticResource WindowBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource FontColor}"/>
            <Setter Property="Padding" Value="5,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentColor}"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource HighlightColor}"/>
                    <Setter Property="Foreground" Value="{StaticResource AccentColor}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Reworked TabItem style using a ControlTemplate so Background/Foreground for the selected tab are visible -->
        <Style TargetType="{x:Type TabItem}">
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Background" Value="{StaticResource PaneBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource FontColor}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="4,4,0,0"
                                SnapsToDevicePixels="True">
                            <ContentPresenter x:Name="Content"
                                              ContentSource="Header"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <!-- bring selected tab to front and change visuals -->
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#D9EEFF"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#5DAEFF"/>
                                <Setter Property="BorderThickness" Value="0,0,0,3"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="Panel.ZIndex" Value="1"/>
                            </Trigger>

                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource BorderColor}"/>
                                <Setter Property="Foreground" Value="{StaticResource FontColor}"/>
                            </Trigger>

                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type ContentPresenter}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Content, Converter={StaticResource TypeConverter}}" Value="String">
                    <Setter Property="ContentTemplate">
                        <Setter.Value>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" Foreground="{StaticResource FontColor}"/>
                            </DataTemplate>
                        </Setter.Value>
                    </Setter>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <DockPanel>

        <Border DockPanel.Dock="Top" 
                Background="{StaticResource PaneBackground}" 
                BorderBrush="{StaticResource BorderColor}" 
                BorderThickness="0,0,0,1" 
                Padding="0" 
                MouseLeftButtonDown="WindowHeader_MouseLeftButtonDown">

            <DockPanel>
                <Grid Height="70" Margin="10,0,10,0" DockPanel.Dock="Top">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left">
                        <Image Source="pack://application:,,,/Images/SQLAtlasLogo.png" Width="50" Height="50" Margin="0,0,8,0" RenderOptions.BitmapScalingMode="HighQuality"/>
                        <TextBlock Text="SQL Atlas" Foreground="{StaticResource AccentColor}" FontSize="24" FontWeight="ExtraBold" FontFamily="Segoe UI Black" VerticalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <Button Content="Disconnect" Click="DisconnectButton_Click" Width="100" Margin="0,0,15,0"/>
                        <Button Content="—" Click="MinimizeButton_Click" Width="30" Height="30" Style="{StaticResource SystemButtonStyle}"/>
                        <Button Content="☐" Click="MaximizeRestoreButton_Click" Width="30" Height="30" Margin="5,0" Style="{StaticResource SystemButtonStyle}"/>
                        <Button Content="✕" Click="CloseButton_Click" Width="30" Height="30" Style="{StaticResource SystemButtonStyle}"/>
                    </StackPanel>
                </Grid>

                <TabControl x:Name="RibbonMenu" DockPanel.Dock="Bottom" Margin="0" 
                            Background="{StaticResource PaneBackground}" BorderThickness="0,1,0,0" 
                            SelectionChanged="RibbonMenu_SelectionChanged">
                    <TabItem Header="Schema Explorer"/>
                    <TabItem Header="Performance"/>
                    <TabItem Header="Security"/>
                    <TabItem Header="Maintenance"/>
                    <TabItem Header="Configuration"/>
                    <TabItem Header="Design and Dev"/>
                    <TabItem Header="High Availability"/>
                </TabControl>

            </DockPanel>
        </Border>

        <StatusBar DockPanel.Dock="Bottom" Background="{StaticResource WindowBackground}" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,1,0,0">
            <StatusBarItem>
                <TextBlock x:Name="CurrentDatabaseStatusBlock" Foreground="{StaticResource FontColor}" Text="[DB: Not Connected]"/>
            </StatusBarItem>
            
            <Separator/>
            
            <StatusBarItem HorizontalAlignment="Right">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Version: " Foreground="{StaticResource FontColor}"/>
                    <TextBlock x:Name="VersionNumberTextBlock" Text="v0.0.0" Foreground="{StaticResource AccentColor}"/>
                    <TextBlock Text=" | Build: " Foreground="{StaticResource FontColor}" Margin="5,0,0,0"/>
                    <TextBlock x:Name="VersionDateTextBlock" Text="N/A" Foreground="{StaticResource FontColor}"/>
                </StackPanel>
            </StatusBarItem>
        </StatusBar>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,1,0" 
                    Background="{StaticResource WindowBackground}">
                <DockPanel>

                    <TextBlock DockPanel.Dock="Top" x:Name="SidebarHeaderTextBlock"
                               Text="SELECT CATEGORY" Margin="10,10,10,5" 
                               FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource AccentColor}"/>

                    <Grid DockPanel.Dock="Top" Margin="10,0,10,10">
                        <TextBox x:Name="SidebarSearchTextBox" Padding="25,5,5,5" 
                                 TextChanged="SidebarSearchTextBox_TextChanged"/>
                        <TextBlock Text="🔍" Foreground="{StaticResource BorderColor}" VerticalAlignment="Center" 
                                   HorizontalAlignment="Left" Margin="5,0,0,0" IsHitTestVisible="False" FontSize="14"/>
                    </Grid>

                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ListBox x:Name="SidebarListBox" 
                                 Margin="10,0,10,10" 
                                 SelectionMode="Extended" 
                                 SelectionChanged="SidebarListBox_SelectionChanged" 
                                 Background="Transparent" 
                                 BorderThickness="0">
                            <ListBox.GroupStyle>
                                <GroupStyle>
                                    <GroupStyle.ContainerStyle>
                                        <Style TargetType="{x:Type GroupItem}">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="{x:Type GroupItem}">
                                                        <Expander IsExpanded="False" Background="{StaticResource PaneBackground}" Foreground="{StaticResource AccentColor}" FontWeight="Bold">
                                                            <Expander.Header>
                                                                <TextBlock Text="{Binding Name}" Foreground="{StaticResource AccentColor}" FontWeight="SemiBold"/>
                                                            </Expander.Header>
                                                            <ItemsPresenter/>
                                                        </Expander>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </GroupStyle.ContainerStyle>
                                </GroupStyle>
                            </ListBox.GroupStyle>
                        </ListBox>
                    </ScrollViewer>

                </DockPanel>
            </Border>

            <Border Grid.Column="1" Background="{StaticResource PaneBackground}" Padding="10">
                <ContentControl x:Name="MainContentHost" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
            </Border>
        </Grid>
    </DockPanel>
</Window>