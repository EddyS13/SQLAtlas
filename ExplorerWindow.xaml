<Window x:Class="DatabaseVisualizer.ExplorerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Database Visualizer" 
        Height="768" Width="1200"
        WindowStyle="None"                  
        AllowsTransparency="True"
        ResizeMode="CanResize"
        Background="{StaticResource WindowBackground}">

    <Window.Resources>
        <Style TargetType="{x:Type ListBoxItem}">
            <Setter Property="Background" Value="{StaticResource WindowBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource FontColor}"/>
            <Setter Property="Padding" Value="5,2"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentColor}"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource HighlightColor}"/>
                    <Setter Property="Foreground" Value="{StaticResource AccentColor}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="{x:Type TabItem}">
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Foreground" Value="{StaticResource FontColor}"/>
            <Setter Property="Background" Value="{StaticResource PaneBackground}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Bd"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        Padding="{TemplateBinding Padding}"
                        CornerRadius="4,4,0,0"
                        SnapsToDevicePixels="True">
                            <ContentPresenter x:Name="Content"
                                      ContentSource="Header"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"
                                      RecognizesAccessKey="True"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentColor}"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                                <Setter TargetName="Bd" Property="BorderThickness" Value="0,0,0,3"/>

                                <Setter Property="Foreground" Value="White"/>
                                <!--<Setter Property="FontWeight" Value="Bold"/>-->
                                <Setter Property="Panel.ZIndex" Value="1"/>
                            </Trigger>

                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource HighlightColor}"/>
                                <Setter Property="Foreground" Value="{StaticResource AccentColor}"/>
                            </Trigger>

                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="True"/>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentColor}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </MultiTrigger>

                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <DockPanel>

        <Border DockPanel.Dock="Top" 
                Background="{StaticResource PaneBackground}" 
                BorderBrush="{StaticResource BorderColor}" 
                BorderThickness="0,0,0,1" 
                Padding="0" 
                MouseLeftButtonDown="WindowHeader_MouseLeftButtonDown">

            <DockPanel>
                <Grid Height="50" Margin="10,0,10,0" DockPanel.Dock="Top">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left">
                        <Image Source="/DatabaseVisualizer;component/Images/Database Visualizer Logo.png" Width="30" Height="30" Margin="0,0,8,0" RenderOptions.BitmapScalingMode="HighQuality"/>
                        <TextBlock Text="DATABASE VISUALIZER" Foreground="{StaticResource AccentColor}" FontSize="22" FontWeight="ExtraBold" FontFamily="Segoe UI Black" VerticalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <Button Content="Disconnect" Click="DisconnectButton_Click" Width="100" Margin="0,0,15,0"/>
                        <Button Content="—" Click="MinimizeButton_Click" Width="30" Height="30" Style="{StaticResource SystemButtonStyle}"/>
                        <Button Content="☐" Click="MaximizeRestoreButton_Click" Width="30" Height="30" Margin="5,0" Style="{StaticResource SystemButtonStyle}"/>
                        <Button Content="✕" Click="CloseButton_Click" Width="30" Height="30" Style="{StaticResource SystemButtonStyle}"/>
                    </StackPanel>
                </Grid>

                <TabControl x:Name="RibbonMenu" DockPanel.Dock="Bottom" Margin="0" 
                            Background="{StaticResource PaneBackground}" BorderThickness="0,1,0,0" 
                            SelectionChanged="RibbonMenu_SelectionChanged">
                    <TabItem Header="Schema Explorer"/>
                    <TabItem Header="Performance"/>
                    <TabItem Header="Security"/>
                    <TabItem Header="Maintenance"/>
                    <TabItem Header="Configuration"/>
                    <TabItem Header="Design and Dev"/>
                    <TabItem Header="High Availability"/>
                    <TabItem Header="General Info"/>
                </TabControl>

            </DockPanel>
        </Border>

        <StatusBar DockPanel.Dock="Bottom" Background="{StaticResource WindowBackground}" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,1,0,0">
            <StatusBarItem HorizontalAlignment="Left">
                <StackPanel Orientation="Horizontal">
                    <TextBlock FontWeight="Bold" Text="Connection: "/>
                    <TextBlock x:Name="CurrentDatabaseTextBlock" Text="[DB: Not Connected]"/>
                </StackPanel>
            </StatusBarItem>

            <StatusBarItem HorizontalAlignment="Right">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Version: " Margin="10,0"/>
                    <TextBlock x:Name="VersionNumberTextBlock" Text="v0.0.0" Foreground="{StaticResource AccentColor}"/>
                    <TextBlock Text=" (" Margin="5,0,0,0"/>
                    <TextBlock x:Name="VersionDateTextBlock" Text="Build Date" Margin="0,0,5,0"/>
                    <TextBlock Text=")"/>
                </StackPanel>
            </StatusBarItem>
        </StatusBar>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,1,0" 
                    Background="{StaticResource WindowBackground}">
                <DockPanel>

                    <TextBlock DockPanel.Dock="Top" x:Name="SidebarHeaderTextBlock"
                               Text="SELECT CATEGORY" Margin="10,10,10,5" 
                               FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource AccentColor}"/>

                    <Grid DockPanel.Dock="Top" Margin="10,0,10,10">
                        <TextBox x:Name="SidebarSearchTextBox" Padding="25,5,5,5" 
                                 TextChanged="SidebarSearchTextBox_TextChanged"/>
                        <TextBlock x:Name="SearchIconTextBlock" Text="🔍" Foreground="{StaticResource BorderColor}" VerticalAlignment="Center" 
                                   HorizontalAlignment="Left" Margin="5,0,0,0" IsHitTestVisible="False" FontSize="14"/>
                    </Grid>

                    <ScrollViewer x:Name="SchemaScrollViewer" VerticalScrollBarVisibility="Auto" DockPanel.Dock="Top">
                        <ListBox x:Name="SchemaListBox" 
                             Background="Transparent"
                             SelectionMode="Extended" 
                             SelectionChanged="SchemaListBox_SelectionChanged" 
                             BorderThickness="0">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Name}" Margin="10,0" Foreground="{StaticResource FontColor}"/>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                            <ListBox.GroupStyle>
                                <GroupStyle>
                                    <GroupStyle.ContainerStyle>
                                        <Style TargetType="{x:Type GroupItem}">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="{x:Type GroupItem}">

                                                        <Expander IsExpanded="False" Foreground="{StaticResource FontColor}">
                                                            <Expander.Header>
                                                                <StackPanel Orientation="Horizontal">
                                                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" Margin="0,5,5,5"/>
                                                                    <TextBlock Text="{Binding ItemCount, StringFormat=({0})}" FontStyle="Italic"/>
                                                                </StackPanel>
                                                            </Expander.Header>

                                                            <ItemsPresenter/>

                                                        </Expander>

                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </GroupStyle.ContainerStyle>

                                    <GroupStyle.Panel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Vertical"/>
                                        </ItemsPanelTemplate>
                                    </GroupStyle.Panel>

                                </GroupStyle>
                            </ListBox.GroupStyle>
                        </ListBox>
                    </ScrollViewer>

                    <ScrollViewer x:Name="ToolsScrollViewer" VerticalScrollBarVisibility="Auto" Visibility="Collapsed">
                        <ListBox x:Name="ToolsListBox" 
                             SelectionMode="Single" 
                             Background="Transparent"
                             SelectionChanged="ToolsListBox_SelectionChanged" 
                             BorderThickness="0">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding .}" Margin="10,3" Foreground="{StaticResource FontColor}"/>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </ScrollViewer>

                </DockPanel>
            </Border>

            <Border Grid.Column="1" Background="{StaticResource PaneBackground}" Padding="10">
                <ContentControl x:Name="MainContentHost" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
            </Border>
        </Grid>
    </DockPanel>
</Window>